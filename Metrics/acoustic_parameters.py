# In process to publish this section of the code (currently close source)

def calculate_acoustic_parameters(folder_path):
    """Calculate all metrics for acoustic parameter evaluation.
    Computes T30 and C50 mean for batch of audios.

    Args:
        folder_path (str): Path to folder with audios to analyze

    Returns:
        float, float: T30_mean, C50_mean
    """
    T30 = []
    C50 = []

    return T30, C50
